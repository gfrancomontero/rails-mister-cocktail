<div class="container">
    <h1>Add to your cocktail</h1>
    <%= simple_form_for([@cocktail, @dose]) do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient %>
    <%= f.button :submit %>
    <% end %>
<%= link_to "back to all cocktails", cocktails_path %>
</div>
